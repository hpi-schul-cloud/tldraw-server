apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: tldraw-server-pod-monitor
  namespace: {{ NAMESPACE }}
  labels:
    app: tldraw-server
spec:
  selector:
    matchLabels:
      app: tldraw-server
  podMetricsEndpoints:
  - port: "metrics"
