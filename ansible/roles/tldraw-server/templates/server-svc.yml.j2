apiVersion: v1
kind: Service
metadata:
  name: tldraw-server-svc
  namespace: {{ NAMESPACE }}
  labels:
    app: tldraw-server
spec:
  type: ClusterIP
  ports:
    # port for WebSocket connection
    - port: 3345
      targetPort: 3345
      protocol: TCP
      name: tldraw-ws
  selector:
    app: tldraw-server
